name: Deploy App with Insecure Secret

on:
  push:
    branches:
      - main
 
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
 
      # ❌ BAD: This exposes the API key directly in the workflow. 
      - name: Set Insecure Secrets (VULNERABLE)
        run: |
          echo "API_KEY=12345-UNSAFE-API-KEY" >> $GITHUB_ENV
 
      - name: Print the secret (only for illustration — *never do this*)
        run: |
          echo "🚨 The API key is: $API_KEY"
